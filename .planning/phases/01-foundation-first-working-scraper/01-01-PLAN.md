---
wave: 1
depends_on: []
files_modified:
  - package.json
  - server.js
  - public/index.html
  - public/app.js
autonomous: true
requirements_addressed:
  - UI-01
  - FILT-03
---

## Objective

Scaffold the Node.js + Express project and create the Walking Skeleton UI — a working local server at `localhost:3000` with a duration selector, Scrape button, and empty results table. No scraper logic yet; the API returns a stub response so the full request/response cycle can be verified.

## Tasks

<task id="T1">
<title>Initialize Node.js project with Express and Playwright</title>
<read_first>
- D:\CarLeasing\.planning\research\STACK.md (recommended stack and versions)
- D:\CarLeasing\.planning\phases\01-foundation-first-working-scraper\01-SKELETON.md (project structure)
</read_first>
<action>
Create package.json at project root with:
- name: "car-leasing-aggregator"
- type: "commonjs"
- scripts: { "start": "node server.js", "dev": "node --watch server.js" }
- dependencies: express@^4.19, playwright@^1.45
- devDependencies: none required for v1

Run: npm install
Run: npx playwright install chromium
</action>
<acceptance_criteria>
- package.json exists at D:\CarLeasing\package.json with "main" or "scripts.start" = "node server.js"
- node_modules/express exists
- node_modules/playwright exists
- `npm start` can be parsed by node without syntax error (node --check server.js)
</acceptance_criteria>
</task>

<task id="T2">
<title>Create Express server with static file serving and stub scrape endpoint</title>
<read_first>
- D:\CarLeasing\package.json (confirm express version installed)
- D:\CarLeasing\.planning\phases\01-foundation-first-working-scraper\01-SKELETON.md (API contract)
</read_first>
<action>
Create server.js at project root:
- require express, path
- app.use(express.json())
- app.use(express.static(path.join(__dirname, 'public')))
- POST /api/scrape: accept { duration, advancePayment } from req.body, return JSON { offers: [], scrapedAt: new Date().toISOString(), errors: [] } (stub — real scraper wired in Plan 03)
- app.listen(3000, () => console.log('Server running at http://localhost:3000'))
- module.exports = app
</action>
<acceptance_criteria>
- server.js exists and `node --check server.js` exits 0
- `node server.js` starts without throwing (test: start process, wait 1s, check port 3000 responds)
- POST /api/scrape with body { duration: 24, advancePayment: 0 } returns HTTP 200 with JSON { offers: [], scrapedAt: string, errors: [] }
- GET / returns HTTP 200 (will serve index.html once created in T3)
</acceptance_criteria>
</task>

<task id="T3">
<title>Create public/index.html — duration selector + Scrape button + results table</title>
<read_first>
- D:\CarLeasing\.planning\phases\01-foundation-first-working-scraper\01-CONTEXT.md (D-08: plain HTML, D-09: null handling)
- D:\CarLeasing\.planning\research\FEATURES.md (table stakes: monthly price primary metric, sort, source attribution)
</read_first>
<action>
Create public/ directory. Create public/index.html:
- Standard HTML5 boilerplate
- Filter bar: label + select#duration (options: 12, 24, 36, 48 months) defaulting to 24
- Button#scrape-btn "Scrape Instacar.gr"
- div#status (empty, used for "Scraping...", error messages)
- table#results with thead: Rank | Brand | Model | Fuel | Monthly (€) | Duration | km/yr | Insurance | Maintenance | Tyres | CO2 | Source
- tbody#results-body (empty)
- div#meta (shows scrape timestamp + result count)
- script src="/app.js"
- No external CSS frameworks; minimal inline styles for readability
</action>
<acceptance_criteria>
- public/index.html exists
- HTML is valid (no unclosed tags)
- select#duration has exactly 4 options: 12, 24, 36, 48
- button#scrape-btn exists
- table#results has thead with column headers including "Monthly (€)" and "Source"
- tbody#results-body exists and is empty
- script tag references /app.js
</acceptance_criteria>
</task>

<task id="T4">
<title>Create public/app.js — client-side fetch, table render, sort by price</title>
<read_first>
- D:\CarLeasing\public\index.html (IDs and table structure to target)
- D:\CarLeasing\.planning\phases\01-foundation-first-working-scraper\01-CONTEXT.md (D-09: CO2 null → "N/A", services boolean display)
</read_first>
<action>
Create public/app.js with vanilla JS (no frameworks, no bundler):
- DOMContentLoaded listener
- scrape-btn click handler: read select#duration value, POST /api/scrape { duration: parseInt, advancePayment: 0 }, handle loading state (disable button, show "Scraping...")
- renderOffers(offers): populate tbody#results-body — one tr per offer with td for each column; monthlyPrice formatted as "€X,XXX"; CO2: offer.co2gKm ?? "N/A"; services: offer.servicesIncluded.insurance ? "✓" : "✗" (same for maintenance, tyres); source: anchor tag linking to offer.sourceUrl
- Sort: clicking "Monthly (€)" th toggles ascending/descending sort by monthlyPrice; re-renders tbody
- Error handling: if fetch fails or response.ok is false, show error in #status
- Show result count and scrapedAt in #meta after render
- sortState variable tracks { column: 'monthlyPrice', dir: 'asc' }
</action>
<acceptance_criteria>
- public/app.js exists
- Clicking #scrape-btn triggers a POST to /api/scrape (verifiable by browser devtools network tab)
- With stub API (empty offers array), #results-body renders 0 rows and #meta shows "0 offers"
- CO2 null renders as "N/A" in the CO2 column
- Services false renders as "✗"
- Clicking "Monthly (€)" header re-sorts rows (verifiable once real data arrives in Plan 03)
- No JavaScript errors in browser console on page load
</acceptance_criteria>
</task>

## must_haves
- [ ] `node server.js` starts on port 3000 without errors
- [ ] Browser at `http://localhost:3000` shows UI with duration selector and Scrape button
- [ ] POST /api/scrape returns valid JSON (stub response is acceptable at this stage)
- [ ] No JavaScript errors in browser console on page load
